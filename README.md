# Bookstore implementation using FLyweight design pattern
This repository holds two projects. One that has an implementation without a design patterns, and one that has an implementation with the flyweight design pattern.

